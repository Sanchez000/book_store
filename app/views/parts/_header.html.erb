<header class="navbar navbar-default">
  <div class="container">
    <div class="navbar-header">
      <button type="button" data-toggle="collapse" data-target="#navbar" class="navbar-toggle pull-left">
        <span class="sr-only"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand">Bookstore</a>
      <a href="#" class="shop-link pull-right visible-xs">
        <span class="shop-icon">
          <span class="shop-quantity">0</span>
        </span>
      </a>
    </div>
    <a href="#" class="shop-link pull-right hidden-xs">
<span class="shop-icon">
<span class="shop-quantity">3</span></span></a>
    <div id="navbar" aria-expanded="false" class="navbar-collapse collapse pl-30">
      <div class="visible-xs">
        <button type="button" aria-label="Close" class="close">
<span aria-hidden="true">&times;</span></button>
        <ul class="nav navbar-nav">
          <li>
            <%= link_to "Home", home_index_path %>
          </li>
          <li>
            <a type="button" data-toggle="collapse" href="#menu" aria-expanded="false" aria-controls="menu">Shop</a>
            <ul id="menu" aria-expanded="false" class="collapse list-unstyled pl-30 mb-10">
              <% @categories.each do |category| %>
                <li>
                  <%= link_to  catalog_index_path(categori_id: category.id), class: "collapse-link" do %>
                    <%= category.title %>
                      <% end %>
                </li>
                <li>
                  <% end %>
            </ul>
            </li>
            <li>
              <a type="button" data-toggle="collapse" href="#account" aria-expanded="false" aria-controls="account">My account</a>
              <ul id="account1" aria-expanded="false" class="collapse list-unstyled pl-30 mb-10">
              </ul>
            </li>
        </ul>
      </div>
      <div class="hidden-xs">
          <span class="navbar-text">
          </span>
            <ul class="nav navbar-nav">
              <li>
                <%= link_to "Home", home_index_path %>
              </li>
              <li class="dropdown">
                <a href="#" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false" class="dropdown-toggle">Shop</a>
                <ul class="dropdown-menu">
                  <% @categories.each do |category| %>
                    <li>
                      <%= link_to catalog_index_path(categori_id: category.id), class: "collapse-link" do %>
                        <%= category.title %>
                          <% end %>
                    </li>
                    <% end %>
                </ul>
              </li>
              <li class="dropdown">
                  <a href="#" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false" class="dropdown-toggle">My account</a>
                  <ul class="dropdown-menu">
                </ul>
              </li>
            </ul>
      </div>
    </div>
  </div>
</header>
